<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lanren.mt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
<!--模拟代码改动2-->
        <meta-data android:name="LOANDATASDK_ID" android:value="LoanData_562068102903975936"/>
        <meta-data android:name="LOANDATASDK_KEY" android:value="LoanData_a2b6c39356987a3f8cd3cf39c46068d4"/>

        <activity android:name=".HandlerActivity" />
        <activity android:name=".CheckBoxActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>